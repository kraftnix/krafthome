return {
  helper = require 'utils.helper',
  lazy_nix = require 'utils.lazy_nix',
  telescope = {
    terminal = require 'utils.telescope.terminal',
    defaults = require 'utils.telescope.defaults',
  },
  keycommands = require 'utils.keycommands',
  vendor = {
    legendary = require 'utils.vendor.legendary',
    folke = require 'utils.vendor.folke',
    middleclass = require 'utils.vendor.middleclass',
  },
}
